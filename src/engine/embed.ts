/**
 * The snippet's entry point: exactly two names, bundled by scripts/build-engine.mjs
 * into the block people copy out of the Export menu.
 */
export { mount } from "./renderer";
export { coerceSpec } from "./spec";
